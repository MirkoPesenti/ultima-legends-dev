<header class="img-name">
    <img src="{{item.img}}" alt="{{item.name}}" title="{{item.name}}" data-edit="img" data-action="editImage" />
    <input type="text" name="name" value="{{item.name}}" />
</header>